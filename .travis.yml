language: node_js
node_js:
  - '9.2.0'
sudo: false
dist: trusty
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install xorriso
  - sudo apt-get install coreutils
script:
  - sh setup.sh
branches:
  only:
    - develop
